# Copy to .env on the AgenticAI host (do NOT commit the real .env).
# Root directory on the Docker host where per-SAE bundles were unzipped:
#   ${SAE_BUNDLE_DIR}/sae-radius01/sae-radius01.crt.pem  etc.
SAE_BUNDLE_DIR=/home/agenticai/SNEHAL_POC

